__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv*/
.pixi/
pixi.lock
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/
.DS_Store
.ipynb_checkpoints/

# Field data: never commit bulk survey data. Scoped to the repo root with a
# leading slash -- a bare `data/` also matches tests/data/, which silently
# excluded the whole regression corpus.
*.SGD
*.sgd
/data/

# The format-sample corpus is deliberately tracked.
!tests/data/
